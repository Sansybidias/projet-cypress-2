pipeline {
    agent {
        docker {
            image 'cypress/included:15.19.0'
            args '-u=root --entrypoint='
            }
    }

    stages {
        stage('installation les dependances') {
            steps {
                sh 'npm install'
            }
        }
         stage('lancement de test') {
            steps {
                sh 'npx run cypress'
            }
        }
    }
}
